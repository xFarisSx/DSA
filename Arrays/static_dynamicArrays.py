# #C++
# int a[20];
# int b[5] {1, 2, 3, 4, 5}

strings = ['a', 'b', 'c' , 'd'] 
# dynamic arrays in worst cases copies the entire array and move it into another space of memory so it may be O(n) and doubles the space to be able to add more elements without copying
